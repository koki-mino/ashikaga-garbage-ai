# 分別データ更新手順

## 更新するファイル

`data/garbage_rules.json`

分別ルールを変えても、AIモデルや画面コードを変更する必要はありません。AIが出した品目IDと、このJSONの品目IDを照合して表示します。

## 更新前に必ず行うこと

1. 足利市公式ホームページの最新情報を確認します。
2. 他自治体、ブログ、まとめサイトを根拠にしません。
3. 変更日、公式URL、ページの更新日をメモします。
4. 数値や条件が分からない場合は推測せず「要公式確認」とします。
5. 名草地区など地域限定ルールの有無を確認します。

## 既存品目の説明を直す

例としてペットボトルを探します。

```json
{
  "id": "pet_bottle",
  "name": "ペットボトル",
  "result": {
    "category": "資源物",
    "instruction": "出し方の短い説明",
    "cautions": [
      "注意1",
      "注意2"
    ]
  },
  "official_url": "https://www.city.ashikaga.tochigi.jp/...",
  "checked_date": "2026-09-10"
}
```

変更する主な場所は次のとおりです。

- `name`：画面に表示する品目名
- `synonyms`：検索に使う別名
- `category`：分別区分
- `instruction`：出し方
- `cautions`：注意事項
- `official_url`：根拠となる足利市公式ページ
- `checked_date`：公式情報を確認した日（西暦 `YYYY-MM-DD`）

## 同義語を追加する

`synonyms` の `[]` 内へ追加します。

```json
"synonyms": ["空き瓶", "ガラスびん", "ビン"]
```

各項目は半角カンマで区切り、文字は半角の二重引用符で囲みます。最後の項目の後ろにカンマを付けないでください。

## 新しい品目を追加する

条件分岐がない簡単な品目は、次をコピーして `items` 配列の末尾へ追加します。

```json
{
  "id": "english_unique_id",
  "name": "表示名",
  "kana": "ひらがな",
  "synonyms": ["別名1", "別名2"],
  "search_hint": "候補一覧に出す短い補足",
  "result": {
    "category": "分別区分",
    "instruction": "出し方",
    "cautions": ["注意事項"]
  },
  "official_url": "https://www.city.ashikaga.tochigi.jp/公式ページ",
  "checked_date": "YYYY-MM-DD",
  "safety_level": "standard"
}
```

`id`は既存品目と重複しない半角英数字とアンダースコアにします。一度AIラベルと対応させたIDは、後から変更しないでください。

## 条件分岐を追加する

大きさ、電池、中身、素材、地域等で変わる場合は、単一の `result` を書かず `decision` を使います。既存の `stuffed_toy`、`small_appliance`、`spray_can`、`food_tray` をコピーして修正するのが安全です。

分岐は複雑になりやすいため、追加する前にWorkへ次を渡してください。

- 品目名
- 分岐条件
- 条件ごとの分別区分と出し方
- 足利市公式URL
- 公式情報を確認した日

## 危険品目

火災・爆発・けがにつながる品目は次を付けます。

```json
"safety_level": "high"
```

画像だけで一つの答えを表示せず、中身・電池・変形等の追加質問を入れます。

## 更新後の確認

1. JSON構文を確認します。
2. 品目ID重複、公式URL、確認日、条件分岐を確認します。
3. 検索で新しい品目名・別名を試します。
4. PCとスマートフォンで結果を確認します。
5. 公式リンクを開き、内容が一致するか再確認します。

Node.jsがある場合は、フォルダで `npm test` を実行すると基本検査ができます。Node.jsがなくても、更新ファイルをWorkへ渡せば検査できます。

## GitHubへ反映する

GitHubで `data/garbage_rules.json` を開き、鉛筆マークで編集するか、新しい同名ファイルをアップロードして `Commit changes` を押します。数分後に公開ページを再読み込みします。

## 更新履歴

ルール変更時は `CHANGELOG.md` に日付、品目、変更理由、公式URLを1行ずつ記録してください。
