# 将来拡張案

初期版を安全に運用し、分別データとAI評価が安定してから、次の順で検討します。

## 優先度A

### 1 AI対象品目の段階的追加

8品目の誤判定傾向を確認してから、1回3～5品目ずつ追加します。似た品目を同時に追加し、未使用画像で旧モデルと比較します。

### 2 分別ルール更新確認

自動スクレイピングで勝手に更新せず、月1回程度、公式ページの更新日とJSONの確認日を人が照合するチェック表を用意します。

### 3 多言語化

やさしい日本語を基準に、英語、ベトナム語、タガログ語、インドネシア語を追加します。翻訳後も数値・条件・危険表示は公式ルールと照合します。

## 優先度B

### 4 PWA・ホーム画面追加

基本ファイルと分別JSONを端末へ一時保存し、通信不良時にも直近データで検索できるようにします。古いルールであることが分かる更新日表示を強化します。

### 5 音声読み上げ

ブラウザ標準の読み上げを使い、分別区分と注意を音声で確認できるようにします。自動再生はせず、利用者が押したときだけ読み上げます。

### 6 QRコード

公開URLを案内チラシ等で開けるQRコードを作ります。非公式サービス表示をQR周辺にも入れます。

## 優先度C

### 7 Google Sheetsによるデータ管理

JSON直接編集が難しくなった場合、個人用Google SheetsからJSONを書き出す補助ツールを検討します。公開サイトがシートへ直接アクセスして個人情報を取得する構造にはしません。

### 8 町名別収集日案内

公式の最新収集日データを正確に維持できる体制が整った場合だけ追加します。住所・位置情報を収集せず、利用者が町名を手動選択する方式を優先します。

### 9 誤判定報告・画像収集

初期版には実装しません。将来実装する場合も、画像を自動送信せず、送信前に内容・利用目的・保存期間・権利・Exif除去を明示し、本人が明示的に選べる構造にします。

### 10 管理者用画面

公開サイトへ認証情報や編集機能を置かず、別のローカル管理ツールまたは安全な認証付き環境として設計します。

## 実装を急がない機能

- ユーザー登録・ログイン
- 広告・課金・アクセス解析
- 自動Webスクレイピング更新
- 数百品目を一度に学習するAI
- 外部生成AIによる自由回答
- サーバー側画像保存

機能数より、誤案内を減らし、公式情報へ確実につなぐことを優先します。
