# AI再学習・改善手順

ユーザー本人がPython、学習画面、設定値を操作する手順ではありません。追加写真をWorkへ渡し、Work側で再学習・評価・組込みを行う流れです。

## 初回学習

1. ユーザーが撮影指示書に沿って写真を撮る
2. 品目別にまとめてWorkへ渡す
3. Workが重複、ぼけ、ラベル、個人情報、Exifを確認
4. 同じ実物が学習用とテスト用へまたがらないよう分割
5. 転移学習を実行
6. 未使用画像で品目別精度と混同行列を作る
7. 低確信度・上位差・その他判定を評価
8. 合格した場合だけモデルを `model/` へ組み込む

## 精度が低い場合

Workは「写真を増やしてください」だけで終わらせません。誤判定画像を次の観点で分類します。

- 形が似ている：空き缶とスプレー缶など
- 色に依存：銀色だけで缶と判断するなど
- 背景に依存：同じ机で撮った品目だけ正解するなど
- 向きに依存：横向き乾電池を認識しないなど
- 大きさに依存：遠い小型物を認識しないなど
- 商品に依存：特定メーカーだけ正解するなど
- その他クラス不足：対象外を既知品目へ高確信度で誤分類するなど

そのうえで、例えば次のように追加撮影を依頼します。

- 「銀色スプレー缶を空き缶へ誤認しています。銀色のスプレー缶2種類を、正面・側面・斜め、白背景・木目背景で各3枚、合計18枚撮ってください。」
- 「横向き乾電池を見逃しています。単3・単4を横向き1本、横向き複数本、手持ちで各4枚、合計12枚撮ってください。」
- 「対象外の金属製品を空き缶へ分類しています。鍋、金属カップ、工具など6種類を各3枚、合計18枚撮ってください。」

## 再学習後の比較

同じテスト画像を使い、旧モデルと新モデルを比較します。

| 指標 | 旧モデル | 新モデル | 判断 |
|---|---:|---:|---|
| 全体正解率 | 記入 | 記入 | 改善・同等・悪化 |
| スプレー缶→空き缶の誤判定 | 記入 | 記入 | 安全上最重要 |
| その他→既知品目の誤判定 | 記入 | 記入 | 高確信度誤判定を確認 |
| 低確信度へ逃がした割合 | 記入 | 記入 | 多すぎないか確認 |
| スマホ推論時間 | 記入 | 記入 | 実用性確認 |

一部の正解率が上がっても、危険な誤判定が増えたモデルは採用しません。

## モデル差替え

Work側で次を行います。

1. 旧モデルを別フォルダへ保管
2. 新しい `model.json`、`metadata.json`、重みファイルを配置
3. `manifest.json` のバージョン・学習日・ラベル対応を更新
4. 画像AI読込み、上位候補、低確信度、手動検索への切替を再テスト
5. `docs/03_AI学習・評価結果.md` と `CHANGELOG.md` を更新

ユーザーは完成ファイルをGitHubへ同名で上書きアップロードし、公開ページで確認します。
