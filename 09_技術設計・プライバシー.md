# 技術設計・プライバシー

## 全体構造

```text
端末内の画像
  ↓
ブラウザ内AIによる品目候補（モデル搭載後）
  ↓
ユーザーが品目を確認
  ↓
大きさ・電池・中身・地区などを追加確認
  ↓
garbage_rules.json の登録済みルール
  ↓
分別区分・出し方・注意・足利市公式リンク
```

AIが分別方法を文章生成したり、分類区分を自由判断したりする経路はありません。

## 3段階の実装

- STEP1：`index.html?mode=manual`。画像プレビューと手動検索だけを使う。
- STEP2：`index.html?mode=ai`。モデル状態を確認し、利用可能なら画像候補を表示する。
- STEP3：`index.html`。公開用。モデル未作成時もSTEP1相当へ自動で切り替わる。

`step1.html` と `step2.html` は同じ完成版コードへモードを付けて開く入口です。アプリ本体を3重に複製していないため、修正箇所が増えません。

## 静的Web構成

- HTML、CSS、JavaScript、JSONだけで公開
- Node.js、npm、React、Vite、TypeScript、Docker、DB、独自サーバーは利用者環境で不要
- GitHub Pagesのブランチ公開へ対応
- 相対URLだけを使い、リポジトリ名付きURLでも動作
- `.nojekyll` を配置

`package.json` は任意の自動テスト用で、外部依存はありません。公開・利用には不要です。

## 検索

検索時に次を正規化します。

- Unicode NFKC（全角英数字など）
- 英字の大文字・小文字
- カタカナからひらがな
- 空白・一部記号

名前、かな、同義語に対し、完全一致、前方・包含一致、2文字単位の類似度を組み合わせます。類似度が低い場合は候補を表示しません。

## 条件分岐

`garbage_rules.json` の `decision` に、質問ノードと選択肢を書きます。選択肢は次の質問または登録済み結果へ進みます。画像AIに次を推測させません。

- 長さ・厚さ
- 電池の有無と取外し可否
- ガスや液体の残量
- 汚れ・におい
- 居住地区
- スプリング等の内部構造

## AIモデル差替え

`model/manifest.json` がモデルの状態、URL、ラベルと品目IDの対応を保持します。

- `not_trained`：準備中表示。AIライブラリを読み込まない。
- `ready`：画像選択時だけTensorFlow.jsとTeachable Machine画像ライブラリをHTTPSで読み込み、ローカルモデルを利用する。

モデルの推論結果は、確信度と1位・2位の差を確認します。条件を満たしても、ユーザーの「はい」がなければ分別結果を表示しません。

## 画像の通信

選択画像は `URL.createObjectURL(file)` で一時的なブラウザ内URLにし、`img`要素へ表示します。画像を引数にした `fetch`、`FormData`、`XMLHttpRequest`、`sendBeacon`、外部API呼出しはありません。

通信する可能性があるものは次だけです。

1. GitHub Pages上のHTML、CSS、JavaScript、JSON、モデルファイル
2. モデルが `ready` の場合に限り、jsDelivrからTensorFlow.js関連ライブラリ
3. ユーザーが押したときの足利市公式ページ

AIライブラリの取得先は `config.js` で固定しています。画像データをjsDelivrへ送信する処理はありません。

## Content Security Policy

HTMLのCSPで、既定の読込先を同一サイトへ限定し、AIライブラリ取得先だけ `cdn.jsdelivr.net` を許可しています。画像は同一サイト、`blob:`、`data:`だけです。フォーム送信とプラグインオブジェクトを禁止しています。

## 保存・追跡

この初期版は次を使用しません。

- Cookie
- localStorage・IndexedDB
- サーバー側保存
- アクセス解析
- 広告
- ログイン
- 位置情報
- カメラの常時アクセス

ファイル選択時だけ、ブラウザ標準の許可画面が開きます。

## 学習画像

モデルに学習画像そのものを同梱する必要はありません。公開フォルダへ学習画像を置かず、`.gitignore` でも `training/photos/` 等を除外します。Workが学習前にExif等メタデータを除去します。

## 外部ライブラリの注意

モデル搭載後は、TensorFlow.js関連スクリプトをCDNから取得します。CDN障害・通信不良時はAIだけが失敗し、アプリは手動検索へ切り替わります。将来、完全オフライン化する場合は、利用条件とファイルサイズを確認してライブラリを同梱します。
